# More Tests, Less Work

TypeScript code examples using [JSVerify](https://github.com/jsverify/jsverify) for my
[_More Tests, Less Work_](https://github.com/goblindegook/talk-more-tests-less-work) talk.

To install dependencies and execute the tests, run:

```shell
$ npm && npm test
```

Or, if using [Yarn](https://yarnpkg.com/):

```shell
$ yarn && yarn test
```
